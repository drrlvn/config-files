[alias]
    ai      = add -i
    ap      = add -p
    br      = branch
    co      = checkout
    ci      = commit
    dc      = diff --cached
    ds      = !git --no-pager diff --stat
    dw      = diff --color-words
    fix     = commit --amend --no-edit
    flush   = !git add -A :/ && git commit -m "flush" && git reset --hard HEAD^
    l       = log --graph --decorate --pretty=oneline --abbrev-commit
    la      = log --graph --decorate --pretty=oneline --abbrev-commit --all
    lg      = log --graph --pretty=format:'%C(yellow)%h%C(bold red)%d%Creset %s %Cgreen(%ad) %C(bold blue)<%an>%Creset' --abbrev-commit --date=relative
    lp      = log --patch
    lpw     = log --patch --color-words
    ls      = log --stat
    ri      = rebase -i
    st      = status -sb
    unstage = reset --

[color "diff"]
    old  = red bold
    new  = blue bold
    frag = magenta bold

[core]
    pager          = less
    sparseCheckout = true
    abbrev = 12

[diff]
    renamelimit      = 0
    renames          = copy
    ignoreSubmodules = dirty

[rebase]
    autosquash = true
    stat = true

[pull]
    ff = only

[user]
    name  = Dror Levin
    email = spatz@psybear.com

[gui]
    fontui = -family \"Open Sans\" -size 10 -weight normal -slant roman -underline 0 -overstrike 0
    fontdiff = -family \"Ubuntu Mono\" -size 12 -weight normal -slant roman -underline 0 -overstrike 0

[svn]
    rmdir = true
